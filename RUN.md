# איך מפעילים את CineMind שירוץ לבד 24/7

מדריך פשוט בעברית. עידו — זה כל מה שצריך כדי שהאתר והמוח (ה-LLM) יעבדו כל הזמן, בלי
להגדיר מחדש בכל פעם.

> בקצרה: שני דברים צריכים לרוץ — **(1) שרת ה-AI המקומי (Ollama)** ו-**(2) האתר**.
> ברגע שמפעילים את משימת ה-Keep-Alive פעם אחת, ה-AI דואג לעצמו (קם מחדש אם נפל,
> נשאר חם ומהיר). גם אם ה-AI ייפול לרגע — המנוע ימשיך לתת המלצות (הליבה דטרמיניסטית,
> ה-AI רק מנסח את ההסבר). אז לקוחות אף פעם לא נתקעים.

## פעם אחת — התקנה (5 דקות)

1. לוודא ש-Ollama מותקן והמודל קיים:
   ```powershell
   ollama list          # צריך להופיע: qwen2.5:14b-instruct
   ```
   אם המודל חסר: `ollama pull qwen2.5:14b-instruct`

2. להפעיל את ה-Keep-Alive האוטומטי (מ-PowerShell עם הרשאות מנהל, פעם אחת):
   ```powershell
   powershell -ExecutionPolicy Bypass -File scripts\install-ollama-keepalive.ps1
   ```
   מעכשיו, בכל הדלקה של המחשב + כל שתי דקות, המערכת בודקת שה-AI חי, מקימה אותו אם
   נפל, ושומרת אותו חם ומהיר. (יומן: `scripts\ollama-keepalive.log`.)

## כל יום — שזה ירוץ

ה-AI כבר דואג לעצמו. צריך רק שהאתר ירוץ:

```powershell
npm run build      # פעם אחת אחרי שינויי קוד
npm start          # מריץ את האתר על http://localhost:3000
```

> טיפ: אפשר גם להפוך את `npm start` למשימה מתוזמנת (אותו עיקרון כמו ה-Keep-Alive) כדי
> שהאתר יעלה לבד בכל הדלקה. אם רוצים — תגיד לי ואוסיף סקריפט.

## איך יודעים שהכל חי

- בריאות ה-AI:
  ```powershell
  (Invoke-WebRequest http://localhost:11434/api/tags -UseBasicParsing).StatusCode   # 200 = חי
  ```
- בריאות המוח באתר: לפתוח `http://localhost:3000/api/brain-health` (מחזיר JSON עם
  סטטוס ה-AI והמודל).

## הגיבוי / "מה אם משהו נופל"

- **ה-AI נפל** → ה-Keep-Alive מקים אותו תוך ~10 שניות, אוטומטית.
- **ה-AI לא זמין בכלל** → המנוע ממשיך לעבוד: הזיהוי וההמלצות דטרמיניסטיים (לא תלויים
  ב-AI), וההסברים נכתבים מתבנית. הלקוח מקבל המלצות תקינות בלי תקלה.
- **TMDB לא זמין** → יש fallbacks לכל שלב, כך שתמיד עולה סרט אמיתי.

## כשיגיעו הלקוחות הראשונים — מעבר לענן

כרגע ה-AI רץ על המחשב שלך (חינם, אבל דורש שהמחשב יהיה דלוק). כשנרצה זמינות מלאה בלי
תלות במחשב — נעביר את ה-AI למודל בענן בשורה אחת של הגדרה (`.env.local`), בלי לגעת
בקוד. תגיד לי ואכין את זה.
